{
  "name": "bighat",
  "version": "1.0.0",
  "author": "Zachary Siswick",
  "license": "ISC",
  "description": "DAG diagram assignment",
  "scripts": {
    "build": "esbuild app.ts --bundle --outfile=index.js",
    "test": "jest --config jest.config.js --env=jsdom"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "esbuild": "0.20.2",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
